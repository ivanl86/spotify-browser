{
  "name": "spotify-browser-app",
  "version": "0.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start-client": "cd client && npm start",
    "start-server": "cd server && npm run start"
  },
  "author": "Ivan Leung",
  "license": "ISC",
  "description": "",
  "private": true,
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
